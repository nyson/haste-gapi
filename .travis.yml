language: haskell
sudo: false

ghc:
  - '7.8'

before_install:
  - cabal install haste-compiler-0.5.3
  - haste-boot
  
install: 
  - haste-cabal install --only-dependencies

script:
  - haste-cabal install
